# === macOS ===
.DS_Store

# === Secrets — NE JAMAIS committer ===
.env
.env.*
!.env.example

# === Python ===
venv/
__pycache__/
*.pyc
*.pyo

# === Données générées du moteur de reco (voir ROADMAP-cerveau.md) ===
# Tout le dossier data/ est régénérable (ingest.py / embed.py / canon.py) — jamais committé.
backend/recommender/data/
*.npy
*.tsv
*.tsv.gz
movies.json
domaines.json
embeddings.npy

# === JavaScript / Node / Expo ===
node_modules/
dist/
.expo/

# === iOS natif (Expo prebuild) ===
ios/Pods/
ios/build/

# === Maquette design : posters générés (base64) + images TMDB (copyright) + build ===
posters.js
posters/
_standalone.html

# === Claude Code (local) ===
.claude/worktrees/
.claude/launch.json
.claude/settings.local.json
_carte.npz
_carte_pacmap.npy
